<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Timeline</title>
</head>
<body>
    <% messages.forEach(message => { %>
        <div>
            <p><strong><%= message.name %> - <%= message.createdAt %></strong></p>
            <p><%= message.message %></p>
            <hr>
        </div>
    <% }) %>
</body>
</html>
